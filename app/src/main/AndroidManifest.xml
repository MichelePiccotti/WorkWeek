<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Nessun permesso necessario per CSV/PDF in getExternalFilesDir -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkWeek"
        >

        <!-- MAIN ACTIVITY (unica con launcher) -->
        <activity
            android:name=".ActivityMain"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ALTRE ACTIVITY (senza duplicati) -->
        <activity
            android:name=".ActivityAddRecord"
            android:exported="false" />

        <activity
            android:name=".ActivitySummary"
            android:exported="false" />

        <activity
            android:name=".ActivityCalendar"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- NOTIFICHE -->
        <receiver
            android:name=".DailyNotificationReceiver"
            android:exported="false" />

    </application>

</manifest>
